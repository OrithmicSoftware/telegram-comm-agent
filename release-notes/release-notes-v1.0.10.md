### v1.0.10

- Added `GET /track?source=&to=` click-tracking redirect endpoint — logs click to admin, redirects to destination; destinations configured via `TRACK_DESTINATIONS`
- Added `GET /` default endpoint that returns an application manifest with app name, version, description, and a list of all available endpoints
---
