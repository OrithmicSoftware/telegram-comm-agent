-### v1.0.5

- Add `/health` endpoint: The built-in Express web server now exposes a GET /health endpoint for health checks and deployment monitoring. Returns `{ status: 'ok' }` with HTTP 200.
- Documentation and release notes consistency for previous versions
---
